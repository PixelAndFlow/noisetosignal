# Used only by the automated test suite (vitest). Never used in dev or prod.
# Points at a local Postgres database, not Neon — see server/tests/README.md
# for setup instructions.

DATABASE_URL=postgresql://techno@localhost:5432/noisetosignal_test?sslmode=disable

GOOGLE_CLIENT_ID=test-client-id
GOOGLE_CLIENT_SECRET=test-client-secret
YOUTUBE_API_KEY=test-api-key

# Fixed dummy values so encryption/JWT round-trip deterministically in tests.
# Generated the same way as server/.env.example describes, just checked in
# here because this is test-only data, never real secrets.
ENCRYPTION_KEY=1cc75b85abffd829971468cc3716641d070e81705af554bda61ad081df6bd787
JWT_SECRET=test-jwt-secret-do-not-use-in-production-0123456789abcdef0123456789abcdef

CLIENT_URL=http://localhost:5173
NODE_ENV=test
PORT=3999
